# Example environment variables for System Monitor
# Copy this file to `.env` and fill in real values. Keep secrets out of source control.

# SMTP (email) configuration
SMTP_HOST=smtp.example.com
# SMTP_PORT is typically 587 for STARTTLS or 465 for SSL
SMTP_PORT=587
SMTP_USER=your-smtp-username@example.com
SMTP_PASSWORD=your-smtp-password

# Optional: timeout for SMTP connections (seconds)
# Uncomment and set if you want to override the default used by the app
# SMTP_TIMEOUT=10

# Screenshot storage directory
# On Windows, use format: C:\\Users\\YourName\\screenshots or Data/screenshots
# On Linux/Mac: /data/screenshots or ./Data/screenshots
SCREENSHOT_DIR=Data/screenshots

# Logging level for the application (optional)
# LOG_LEVEL=INFO
